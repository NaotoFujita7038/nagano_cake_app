<p><b>商品一覧</b>
全(<%=Item.count%>)件</p>
<% @items.each do |item| %>
  <div>
    <p>
    <%=link_to item_path(item.id) do %>
    <%= image_tag 'チーズケーキ.jpg', class: 'hoge' %>
    <% end %>
    <p>商品名：<%=item.name%></p>
    <p>￥<%=item.price%></p>
  </div>
<% end %>